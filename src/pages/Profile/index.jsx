import React from "react"
import styled from "styled-components"

const ProfileWrap = styled.div`
  min-height: 100vh;
`

export default function Profile() {
  return <ProfileWrap>Your in Profile</ProfileWrap>
}
